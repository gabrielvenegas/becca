import { MicroframeworkSettings } from "microframework";

export const iocLoader = (settings: MicroframeworkSettings | undefined) => {
  // const audienceService: AudienceService = new AudienceService();
  // const campaignService: CampaignService = new CampaignService();
  // const authService: AuthService = new AuthService();
  // const userService: UserService = new UserService();
  // Container.set(AudienceService, audienceService);
  // Container.set(CampaignService, campaignService);
  // Container.set(AuthService, authService);
  // Container.set(UserService, userService);
};
